#
#   Apache htaccess file for url rewriting
#
<IfModule mod_rewrite.c>
	RewriteEngine On
    RewriteBase /

    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule . hostdog.php [L]
</IfModule>
